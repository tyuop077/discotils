import styles from "./footer.module.scss";

const Footer = () => (
    <footer className={styles.footer}>
        <div className={styles.container}>
            Discord Utilities • Not affiliated with Discord.
        </div>
    </footer>
)

export default Footer
